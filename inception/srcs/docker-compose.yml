services:
  nginx:
    restart: always
    build: requirements/nginx/
    image: nginx
    container_name: nginx
    env_file:
      - .env
      - ../secrets/credentials.txt
      - ../secrets/db_password.txt
      - ../secrets/db_root_password.txt
    ports:
      - "443:443"
    networks:
      - inception
    volumes:
      - wordpressv:/var/www/wordpress

  mariadb:
    restart: always
    build: requirements/mariadb/
    image: mariadb
    container_name: mariadb
    env_file:
      - .env
      - ../secrets/credentials.txt
      - ../secrets/db_password.txt
      - ../secrets/db_root_password.txt
    networks:
      - inception
    volumes:
      - mariadbv:/var/lib/mysql

  wordpress:
    restart: always
    build: requirements/wordpress/
    image: wordpress
    container_name: wordpress
    env_file:
      - .env
      - ../secrets/credentials.txt
      - ../secrets/db_password.txt
      - ../secrets/db_root_password.txt
    networks:
      - inception
    volumes:
      - wordpressv:/var/www/wordpress
    depends_on:
      - mariadb

volumes:
  mariadbv:
    name: mariadbv
    driver: local
    driver_opts:
      type: 'none'
      o: 'bind'
      device: '/home/oumondad/data/mariadb'

  wordpressv:
    name: wordpressv
    driver: local
    driver_opts:
      type: 'none'
      o: 'bind'
      device: '/home/oumondad/data/wordpress'

networks:
  inception:
    name: 'inception'